<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Liste des enseignes</title>
    </head>
    <body>
    {% if pEnseigne != null %}
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
            
        LISTE DES ENSEIGNES</br>
        <table>
        {% for e in pEnseigne %}
        <tr>
            <td><a href="{{ path('enseigneConsulter', { 'id': e.id }) }}">{{ e.libelle }}</a></td>
    {% else %}
        <tr>
        <td>Aucune enseigne n'a été trouvée.</td>
        </tr>
        {% endfor %}
    {% endif %}
</table>
	
	 
    </body>
</html>