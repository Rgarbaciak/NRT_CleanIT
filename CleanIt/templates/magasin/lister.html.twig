<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Liste des magasins</title>
    </head>
    <body>
    {% if pMagasin != null %}
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
            
        LISTE DES ENSEIGNES</br>
        <table>
        {% for e in pMagasin %}
        <tr>
            <td><a href="{{ path('magasinConsulter', { 'id': e.id }) }}">{{ e.id }}</a></td>
    {% else %}
        <tr>
        <td>Aucune magasin n'a été trouvée.</td>
        </tr>
        {% endfor %}
    {% endif %}
</table>
	
	 
    </body>
</html>